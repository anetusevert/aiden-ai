feat: cinematic UI overhaul with glassmorphic styling and smart onboarding

Add 1800+ lines of glassmorphic CSS using existing design tokens for all
case-centric components: pages, modals, panels, cards, tables, badges,
forms, skeleton loaders, timelines, and upload zones.

- Glass-styled pages: dashboard stat cards with breathing glow, case
  tables with priority dot pulse, client grid cards with hover shadows
- Skeleton loaders: shimmer animations replace all "Loading..." text
  across dashboard, clients, cases, detail pages, and Rail2 panels
- Modal polish: glass backdrop blur, styled form fields with focus glow,
  type selector cards with descriptions, client suggestion dropdown
- Empty states: SVG icon containers with gold CTA buttons
- Smart client onboarding: POST /api/v1/clients/extract-from-document
  endpoint uses text extraction + LLM to auto-fill client forms from
  uploaded CR certificates, Wakala, national IDs, or any document
- Upload zone in NewClientModal with drag-and-drop, progress bar,
  and automatic form pre-fill from AI extraction
- Case detail: Create Document button that creates office doc via
  Collabora and auto-attaches it to the case
